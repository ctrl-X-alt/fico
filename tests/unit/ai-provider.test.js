const test=require('node:test');const assert=require('node:assert/strict');const{OpenAICompatibleProvider}=require('../../apps/api/src/ai/provider');test('provider configures models and retry budget',()=>{const p=new OpenAICompatibleProvider({apiKey:'x',baseUrl:'http://x/v1',model:'m',visionModel:'v',timeoutMs:1,maxRetries:2});assert.equal(p.model,'m');assert.equal(p.visionModel,'v');assert.equal(p.maxRetries,2)});