const test=require("node:test"),assert=require("node:assert/strict"),{safeId}=require("../../apps/api/src/security"),{validateUpload}=require("../../apps/api/src/validation");test("safeId blocks traversal",()=>{assert.equal(safeId("../x"),null);assert.equal(safeId("abc-123"),"abc-123")});test("upload validates signatures",()=>{assert.equal(validateUpload({mimetype:"image/png",buffer:Buffer.from("x")}).ok,false);assert.equal(validateUpload({mimetype:"image/png",buffer:Buffer.from([137,80,78,71,13,10,26,10])}).ok,true)});