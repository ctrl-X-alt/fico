const test=require("node:test");const assert=require("node:assert/strict");const{checkAndConsume}=require("../../apps/api/src/usage");test("allows two analyses and blocks third",()=>{const m=new Map(),u=checkAndConsume(m,"u",2,new Date("2026-09-01"));assert.equal(u.used,1);assert.equal(checkAndConsume(m,"u",2,new Date("2026-09-02")).allowed,true);assert.equal(checkAndConsume(m,"u",2,new Date("2026-09-03")).allowed,false);});