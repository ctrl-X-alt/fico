const ALLOWED_STATUS=new Set(['meaningful_friction_signal','potential_friction','normal_or_expected_behavior','insufficient_evidence','data_quality_issue','measurement_window_issue','segmentation_required','evidence_conflict','technical_cause_possible','intentional_qualification_possible','delayed_activation_possible','no_meaningful_friction_established']);const CAUSAL_WORDS=/\b(proves?|proven|definitely|certainly|users are confused|users hate|clearly broken|causes?)\b/i;
function sanitizeText(text){return typeof text==='string'?text.replace(/[\\u0000-\\u0008\\u000B\\u000C\\u000E-\\u001F]/g,' ').replace(/\s+/g,' ').trim().slice(0,10000):''}
function validateDiagnosis(r){const e=[];if(!ALLOWED_STATUS.has(r?.diagnosis_status))e.push('invalid diagnosis_status');if(typeof r?.confidence?.score!=='number'||r.confidence.score<0||r.confidence.score>100)e.push('confidence.score must be 0..100');for(const g of ['facts','hypotheses','uncertainties','findings','recommendations','next_evidence'])if(!Array.isArray(r?.[g]))e.push(g+' must be an array');const txt=JSON.stringify(r?.findings||[])+JSON.stringify(r?.recommendations||[]);if(CAUSAL_WORDS.test(txt))e.push('causal language requires evidence-linked rewrite');for(const g of ['facts','hypotheses','findings','recommendations','next_evidence']){const a=r?.[g]||[];if(a.some(x=>x==null||(typeof x!=='string'&&typeof x!=='object')))e.push(g+' contains invalid item');}return{valid:e.length===0,errors:e};}
module.exports={ALLOWED_STATUS,validateDiagnosis,sanitizeText};