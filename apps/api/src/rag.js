const fs=require("node:fs/promises");const path=require("node:path");async function loadKnowledge(root){const out=[];async function walk(dir){for(const e of await fs.readdir(dir,{withFileTypes:true})){const p=path.join(dir,e.name);if(e.isDirectory())await walk(p);else if(e.name.endsWith(".md"))out.push({path:p,text:await fs.readFile(p,"utf8")})}}try{await walk(root)}catch{return[]}return out}function retrieve(docs,q,k=5){const terms=new Set(String(q||"").toLowerCase().split(/\W+/).filter(x=>x.length>3));return docs.map(d=>({d,s:[...terms].reduce((n,t)=>n+(d.text.toLowerCase().includes(t)?1:0),0)})).filter(x=>x.s>0).sort((a,b)=>b.s-a.s).slice(0,k).map(x=>x.d)}module.exports={loadKnowledge,retrieve};