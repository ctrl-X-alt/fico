const crypto=require("node:crypto");const{getRepository}=require("./repository");const COOKIE="friction_session";function sign(payload,secret){const body=Buffer.from(JSON.stringify(payload)).toString("base64url"),sig=crypto.createHmac("sha256",secret).update(body).digest("base64url");return body+"."+sig}function verify(token,secret){const[body,sig]=String(token||"").split(".");if(!body||!sig)return null;const expected=crypto.createHmac("sha256",secret).update(body).digest("base64url"),a=Buffer.from(sig),b=Buffer.from(expected);if(a.length!==b.length||!crypto.timingSafeEqual(a,b))return null;try{const p=JSON.parse(Buffer.from(body,"base64url").toString());if(p.exp&&Date.now()/1000>p.exp)return null;return p}catch{return null}}function parseCookies(req){const out={};for(const part of String(req.headers.cookie||"").split(";")){const i=part.indexOf("=");if(i>0)out[part.slice(0,i).trim()]=decodeURIComponent(part.slice(i+1).trim())}return out}function getUser(req){const secret=process.env.AUTH_SECRET,token=parseCookies(req)[COOKIE]||(req.headers.authorization||"").replace(/^Bearer\s+/i,"");if(secret&&token){const p=verify(token,secret);if(p?.sub)return String(p.sub).slice(0,128)}if(process.env.NODE_ENV!=="production"){const id=req.headers["x-user-id"];if(id)return String(id).slice(0,128)}return null}function setSession(res,userId){if(!process.env.AUTH_SECRET)throw Object.assign(new Error("auth_secret_not_configured"),{code:"auth_secret_not_configured"});const token=sign({sub:userId,iat:Math.floor(Date.now()/1000),exp:Math.floor(Date.now()/1000)+604800},process.env.AUTH_SECRET),secure=process.env.NODE_ENV==="production";res.setHeader("Set-Cookie",COOKIE+"="+encodeURIComponent(token)+"; Path=/; HttpOnly; SameSite=Lax; Max-Age=604800"+(secure?"; Secure":""))}function clearSession(res){res.setHeader("Set-Cookie",COOKIE+"=; Path=/; HttpOnly; SameSite=Lax; Max-Age=0"+(process.env.NODE_ENV==="production"?"; Secure":""))}async function signInWithGoogle(code){const{id,secret,redirect}= {id:process.env.GOOGLE_CLIENT_ID,secret:process.env.GOOGLE_CLIENT_SECRET,redirect:process.env.GOOGLE_REDIRECT_URI};if(!id||!secret||!redirect)throw Object.assign(new Error("google_oauth_not_configured"),{code:"google_oauth_not_configured"});const tr=await fetch("https://oauth2.googleapis.com/token",{method:"POST",headers:{"Content-Type":"application/x-www-form-urlencoded"},body:new URLSearchParams({code,client_id:id,client_secret:secret,redirect_uri:redirect,grant_type:"authorization_code"})});if(!tr.ok)throw Object.assign(new Error("google_token_exchange_failed"),{code:"google_token_exchange_failed"});const t=await tr.json();const ur=await fetch("https://www.googleapis.com/oauth2/v3/userinfo",{headers:{Authorization:"Bearer "+t.access_token}});if(!ur.ok)throw Object.assign(new Error("google_userinfo_failed"),{code:"google_userinfo_failed"});const p=await ur.json(),now=new Date().toISOString(),user={id:String(p.sub),email:p.email||null,name:p.name||null,picture:p.picture||null,updatedAt:now},rp=getRepository();if(rp.kind==="mongo")await rp.mongo.users().updateOne({id:user.id},{$set:user,$setOnInsert:{createdAt:now}},{upsert:true});else rp.memory.users.set(user.id,{...(rp.memory.users.get(user.id)||{}),...user});return{user}}module.exports={getUser,requireAuth:(req,res,next)=>{const id=getUser(req);if(!id)return res.status(401).json({error:"authentication_required"});req.userId=id;next()},signInWithGoogle,setSession,clearSession};