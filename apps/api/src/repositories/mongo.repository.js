const {MongoClient}=require("mongodb");
class MongoRepository{constructor(uri,dbName="fico"){this.uri=uri;this.dbName=dbName;this.client=null;this.db=null}
async connect(){if(this.db)return this.db;this.client=new MongoClient(this.uri,{maxPoolSize:Number(process.env.MONGO_MAX_POOL_SIZE||20),minPoolSize:Number(process.env.MONGO_MIN_POOL_SIZE||2),serverSelectionTimeoutMS:Number(process.env.MONGO_SERVER_SELECTION_TIMEOUT_MS||5000),connectTimeoutMS:Number(process.env.MONGO_CONNECT_TIMEOUT_MS||5000)});await this.client.connect();this.db=this.client.db(this.dbName);await Promise.all([this.analyses().createIndex({ownerId:1,createdAt:-1}),this.analyses().createIndex({ownerId:1,id:1},{unique:true}),this.analyses().createIndex({status:1,leaseExpiresAt:1}),this.users().createIndex({id:1},{unique:true}),this.users().createIndex({email:1},{unique:true,sparse:true}),this.usage().createIndex({userId:1,period:1},{unique:true})]);return this.db}
async close(){if(this.client)await this.client.close();this.client=null;this.db=null}
analyses(){return this.db.collection("analyses")}users(){return this.db.collection("users")}usage(){return this.db.collection("usage")}knowledge(){return this.db.collection("knowledge_chunks")}
async createAnalysis(item){await this.analyses().insertOne({...item,_id:item.id});return item}
async getAnalysis(id,ownerId){return this.analyses().findOne({_id:id,ownerId},{projection:{_id:0}})}
async listAnalyses(ownerId){return this.analyses().find({ownerId},{projection:{_id:0}}).sort({createdAt:-1}).toArray()}
async updateAnalysis(id,ownerId,patch){return this.analyses().findOneAndUpdate({_id:id,ownerId},{$set:patch},{returnDocument:"after",projection:{_id:0}})}
async acquireLease(id,ttl){const now=new Date(),exp=new Date(now.getTime()+ttl),leaseId=require("node:crypto").randomUUID();const r=await this.analyses().findOneAndUpdate({_id:id,$or:[{leaseExpiresAt:{$exists:false}},{leaseExpiresAt:null},{leaseExpiresAt:{$lte:now}}],status:{$ne:"completed"}},{$set:{status:"running",leaseId,leaseExpiresAt:exp}},{returnDocument:"after",projection:{_id:0}});return r?{leaseId,expiresAt:exp.toISOString()}:null}
async releaseLease(id,leaseId){const r=await this.analyses().updateOne({_id:id,leaseId},{$set:{leaseExpiresAt:null},$unset:{leaseId:""}});return r.modifiedCount===1}
async reserveUsage(userId,period,limit=2){const now=new Date().toISOString();const existing=await this.usage().findOneAndUpdate({userId,period,$expr:{$lt:[{$add:[{$ifNull:["$used",0]},{$ifNull:["$reserved",0]}]},limit]}},{$inc:{reserved:1},$set:{updatedAt:now,limit}},{returnDocument:"after",projection:{_id:0}});if(existing)return{allowed:true,used:existing.used||0,reserved:existing.reserved||0,limit,remaining:Math.max(0,limit-(existing.used||0)-(existing.reserved||0)),period};try{await this.usage().insertOne({userId,period,used:0,reserved:1,limit,createdAt:now,updatedAt:now});return{allowed:true,used:0,reserved:1,limit,remaining:limit-1,period}}catch(e){if(e?.code!==11000)throw e}const retry=await this.usage().findOneAndUpdate({userId,period,$expr:{$lt:[{$add:[{$ifNull:["$used",0]},{$ifNull:["$reserved",0]}]},limit]}},{$inc:{reserved:1},$set:{updatedAt:now,limit}},{returnDocument:"after",projection:{_id:0}});const cur=retry||await this.usage().findOne({userId,period},{projection:{_id:0}});const used=cur?.used||0,reserved=cur?.reserved||0;return retry?{allowed:true,used,reserved,limit,remaining:Math.max(0,limit-used-reserved),period}:{allowed:false,used,reserved,limit,remaining:Math.max(0,limit-used-reserved),period}}
async finalizeUsage(userId,reservation,success){const period=typeof reservation==="string"?reservation:reservation.period,now=new Date().toISOString(),inc=success?{reserved:-1,used:1}:{reserved:-1};const r=await this.usage().findOneAndUpdate({userId,period,reserved:{$gt:0}},{$inc:inc,$set:{updatedAt:now}},{returnDocument:"after",projection:{_id:0}});if(!r)throw Object.assign(new Error("usage_reservation_missing"),{code:"usage_reservation_missing"});return{allowed:true,used:r.used||0,reserved:r.reserved||0,limit:r.limit||2,remaining:Math.max(0,(r.limit||2)-(r.used||0)-(r.reserved||0)),period}}
async cleanupExpiredReservations(maxAgeMs=1800000){const cutoff=new Date(Date.now()-maxAgeMs).toISOString();return this.usage().updateMany({reserved:{$gt:0},updatedAt:{$lt:cutoff}},{$set:{reserved:0,updatedAt:new Date().toISOString()}})}
async cleanupExpiredLeases(now=new Date()){return this.analyses().updateMany({status:"running",leaseExpiresAt:{$lte:now}},{$set:{status:"draft",leaseExpiresAt:null},$unset:{leaseId:""}})}
}
module.exports={MongoRepository};