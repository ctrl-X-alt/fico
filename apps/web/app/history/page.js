"use client";import {useEffect,useState} from "react";import {listAnalyses} from "../../lib/api";export default function History(){const [items,setItems]=useState([]);useEffect(()=>{listAnalyses().then(setItems)},[]);return <main className="shell"><nav className="nav"><div className="brand">Friction</div><a href="/">New analysis</a></nav><div className="eyebrow">Diagnostic Dossier</div><h1 style={{fontSize:48}}>History</h1>{items.map(x=><a key={x.id} className="card" style={{display:"block",margin:"12px 0",textDecoration:"none",color:"inherit"}} href={"/analysis/"+x.id}><strong>{x.input?.business?.name||"Untitled analysis"}</strong><div>{x.status}</div></a>)}</main>}